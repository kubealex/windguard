# TODO

- organize the folders by application (dashboard, core, etc)
- add instructions to build and create container images
- Provide manifests for each component to replace the provisional manifests in the fleet directory.